# The dist/ directory is populated at build time by `make build-spa`.
# Only the .gitkeep placeholder is tracked so the embed compiles on
# fresh clones (IsBuilt() returns false until build-spa is run).
# All other dist/* contents are build artifacts — do not commit them.
!dist/
dist/*
!dist/.gitkeep
